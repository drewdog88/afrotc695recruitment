# Minimal requirements to stay under 250MB limit
Flask>=3.0.0,<4.0.0
Flask-SQLAlchemy>=3.0.0,<4.0.0
Werkzeug>=3.0.0,<4.0.0
python-dotenv>=1.0.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0

# Essential authentication only
bcrypt>=4.0.0,<5.0.0
pyotp>=2.9.0,<3.0.0
qrcode>=7.4.0,<8.0.0
Pillow>=10.0.0,<11.0.0
cryptography>=41.0.0,<42.0.0

# R2 Storage - minimal boto3
boto3>=1.34.0,<2.0.0
botocore>=1.34.0,<2.0.0

# Vercel Blob (for documents and code coverage)
vercel-blob>=0.4.0,<1.0.0

# Backup system dependencies (R2 storage only)
schedule>=1.2.0,<2.0.0
requests>=2.31.0,<3.0.0
psutil>=5.9.0,<6.0.0
python-dateutil>=2.8.0,<3.0.0

# Lightweight data export alternatives (much smaller than pandas/reportlab/openpyxl)
xlsxwriter>=3.0.0,<4.0.0  # Excel export (~5-10MB vs openpyxl's 20-30MB)
fpdf2>=2.7.0,<3.0.0       # PDF export (~2-5MB vs reportlab's 50-100MB)
# CSV export uses built-in 'csv' module (0MB)

# Note: pandas, numpy, reportlab, openpyxl removed to reduce bundle size
# These lightweight alternatives provide export functionality at ~10-15MB total
# vs the original heavy packages at ~200MB+

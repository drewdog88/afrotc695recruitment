# Minimal requirements for testing deployment
Flask==3.1.1
Flask-SQLAlchemy==3.0.5
Werkzeug==3.1.3
python-dotenv==1.1.1
vercel-blob==0.4.2
psycopg2-binary==2.9.10

# Essential packages for functionality
bcrypt==4.0.1
schedule==1.2.0

# Security packages (lightweight)
Flask-Talisman==1.1.0
Flask-WTF==1.2.1
Flask-Limiter==3.5.0

# 2FA Authentication Dependencies (minimal)
pyotp==2.9.0
qrcode==7.4.2
Pillow==10.0.0

# Remove heavy packages that can be replaced:
# - pandas (use built-in csv module instead)
# - reportlab (use simpler PDF generation or remove if not critical)
# - cryptography (pyotp handles this)
# - openpyxl (use csv export instead)

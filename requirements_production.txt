# Production requirements - optimized for Vercel serverless (250MB limit)
# This file excludes heavy packages that are not essential for core functionality

# Core Flask framework
Flask==3.1.2
Flask-SQLAlchemy==3.1.1
Werkzeug==3.1.3
python-dotenv==1.1.1

# Database
psycopg2-binary==2.9.10

# Authentication and security
bcrypt==4.0.1
pyotp==2.9.0
qrcode==7.4.2
Pillow==11.3.0
cryptography==46.0.5
zipp==3.19.1
fonttools==4.43.0

# R2 Storage
boto3==1.34.0
botocore==1.34.0

# Vercel Blob (for compatibility)
vercel-blob==0.4.2

# Lightweight export alternatives
xlsxwriter==3.1.9
fpdf2==2.7.8

# Note: pandas, numpy, openpyxl excluded to reduce bundle size
# reportlab@4.4.3+ pinned to avoid vulnerable pillow dependency
reportlab>=4.4.3,<5.0.0
# These can be added back if data export functionality is needed in production

# Task ID: 22
# Title: Fix Authentication and Session Handling for /admin/system-statistics Route
# Status: pending
# Dependencies: 1, 18
# Priority: medium
# Description: Investigate and resolve the issue where the /admin/system-statistics page incorrectly displays the login form instead of the statistics dashboard, ensuring proper session handling and route authentication for authenticated admin users.
# Details:
1. Audit the authentication and session management logic for the /admin/system-statistics route, focusing on how session state is checked and how authenticated users are identified. 
2. Review middleware, server-side, and client-side authentication checks to ensure that authenticated admin users are not erroneously redirected to the login page. Pay special attention to how session cookies are read and validated, and whether the session persists correctly across requests, especially in serverless or statically deployed environments.
3. Confirm that the route's access control logic verifies both authentication and admin authorization, following best practices for securing admin routes (e.g., checking session validity and user role before rendering sensitive data)[4][5].
4. Investigate any recent changes to session storage, cookie configuration, or deployment environment (such as migration to serverless or static hosting) that may affect session persistence or route protection[3].
5. Update the route handler or middleware to ensure that, when a valid session is present and the user has admin privileges, the statistics dashboard is rendered with database statistics, performance metrics, and system health data. If the session is invalid or the user lacks admin rights, redirect to the login page or display an appropriate error message.
6. Refactor code as needed to separate authentication checks from data fetching logic, and ensure that all sensitive data is only accessible to authorized users. Add or update tests to cover both successful and failed authentication scenarios.

# Test Strategy:
1. Log in as an admin user and navigate to /admin/system-statistics; verify that the statistics dashboard is displayed with all expected data (database statistics, performance metrics, system health).
2. Log out or use a non-admin account and attempt to access the route; confirm that the login form or an appropriate error message is shown.
3. Test session persistence by refreshing the page and navigating between routes to ensure authenticated users remain logged in and unauthorized users cannot access the dashboard.
4. Review server and client logs for authentication or session errors during access attempts.
5. If applicable, deploy to a staging environment and repeat the above tests to confirm correct behavior in the production-like context.
